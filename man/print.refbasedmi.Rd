% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{print.refbasedmi}
\alias{print.refbasedmi}
\alias{summary.refbasedmi}
\title{Print and summarise RefBasedMI results}
\usage{
\method{print}{refbasedmi}(x, ...)

\method{summary}{refbasedmi}(object, ...)
}
\arguments{
\item{x, object}{An object returned by \code{\link[=RefBasedMI]{RefBasedMI()}}.}

\item{...}{Passed on to the underlying data-frame method.}
}
\value{
\code{print()} returns its argument invisibly. \code{summary()} returns an
object of class \code{"summary.refbasedmi"}, a list with components \code{method},
\code{reference}, \code{M}, \code{K0}, \code{K1}, \code{delta}, \code{dlag}, \code{depvar}, \code{n_rows},
\code{rows_per_imputation}, and \code{missing_depvar_per_imputation}.
}
\description{
\code{\link[=RefBasedMI]{RefBasedMI()}} returns its stacked long-format imputations as a data frame
of class \code{"refbasedmi"} carrying the run settings as attributes. \code{print()}
shows a one-line description of the run before the data; \code{summary()}
reports the settings together with the row count and the number of missing
outcome values in each imputed dataset (which should be zero for every
\code{.imp > 0}).
}
\details{
Subsetting keeps the class and the stored settings. If the attributes have
been removed, both methods fall back to the plain data-frame behaviour.
}
\examples{
\donttest{
asthmaJ2R <- RefBasedMI(data = asthma, depvar = fev, treatvar = treat,
                        idvar = id, timevar = time, covar = base,
                        method = "J2R", reference = 1, M = 2, seed = 101,
                        burnin = 100, verbose = FALSE)
summary(asthmaJ2R)
head(asthmaJ2R)
}

}
\seealso{
\code{\link[=RefBasedMI]{RefBasedMI()}}, \code{\link[=as_mids]{as_mids()}}
}
