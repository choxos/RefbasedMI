% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{as_mids}
\alias{as_mids}
\title{Convert a RefBasedMI result to a mice::mids object}
\usage{
as_mids(x)
}
\arguments{
\item{x}{An object returned by \code{\link[=RefBasedMI]{RefBasedMI()}} (or any stacked long-format
data frame with an \code{.imp} column, where \code{.imp = 0} is the original data).}
}
\value{
A \code{\link[mice:mids]{mice::mids()}} object holding the \code{M} imputed datasets.
}
\description{
A convenience wrapper around \code{\link[mice:as.mids]{mice::as.mids()}} so the imputations can be
analysed with \code{\link[mice:with.mids]{mice::with.mids()}} and pooled by Rubin's rules with
\code{\link[mice:pool]{mice::pool()}}. Requires the suggested package \pkg{mice}.
}
\examples{
\donttest{
if (requireNamespace("mice", quietly = TRUE)) {
  asthmaJ2R <- RefBasedMI(data = asthma, depvar = fev, treatvar = treat,
                          idvar = id, timevar = time, covar = base,
                          method = "J2R", reference = 1, M = 5, seed = 101,
                          burnin = 100, verbose = FALSE)
  fit <- with(as_mids(asthmaJ2R),
              lm(fev ~ factor(treat), subset = (time == 12)))
  summary(mice::pool(fit))
}
}

}
\seealso{
\code{\link[=RefBasedMI]{RefBasedMI()}}, \code{\link[mice:as.mids]{mice::as.mids()}}, \code{\link[mice:pool]{mice::pool()}}
}
